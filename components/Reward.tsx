import React from 'react'
import { View, StyleSheet } from 'react-native'

export default () => <View style={Style.container}></View>

const Style = StyleSheet.create({
    container: {
        flex: 1,
    },
})
